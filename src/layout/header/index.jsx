import "./index.scss";
import Button from "component/button";

const Header = ({ test }) => {
  return (
    <div className="header">
      <Button name="Home" />
    </div>
  );
};

export default Header;
